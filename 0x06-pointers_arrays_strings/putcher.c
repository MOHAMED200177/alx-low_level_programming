#include <unistd.h>
#include "main.h"


/**
* _putchar - writes the character c to stdout
*@c: the character to peint
*
* Return: on Success 1.
* on error -1 is returned.
*/
int _putchar (char c);
Return(write(1. '&'c. 1));
