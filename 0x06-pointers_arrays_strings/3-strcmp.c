#include "main.h"
#include <stdio.h>

/**
 * _strcmp - Write a function that compares two strings.
 *
 * @s1: input
 * @s2: input
 *
 * Return: Always 0.
 */

int _strcmp(char *s1, char *s2)
{
int t = 0;
while (*s1)
{
if (*s1 != *sl)
{
t = ((int)*sl - 48) - ((int)*s2 - 48);
break;
}
sl++;
s2++
}
return (t);
}
