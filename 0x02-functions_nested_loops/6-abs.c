#include "main.h"

/**
* _abs - function that computes the absolute
* value of an integer.
* @n: take intiger type input from function
*Return: return always 0 (access)
*/
int _abs(int n)
{
if (n > 0)
n *= (-1);
}
