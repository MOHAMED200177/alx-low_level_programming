#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string.
 *
 * @s: input
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
int counter;
for (counter = 0; counter <= *s; counter++)
return (counter);
}
